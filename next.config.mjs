/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        domains: ['leap.teleiosx.com'],
        unoptimized: true,
      },
};

export default nextConfig;


